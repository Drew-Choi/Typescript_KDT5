let numOne: number = 1;
let numTwo: number = 2;

function add (num1: number, num2: number): number {
  console.log(num1 + num2);
  return num1 + num2;
}

add(numOne, numTwo)

//tuple
let tuple: [string, number, boolean, object] = ['1', 10, true, {name: 'he'}]
console.log(tuple);

//Enum
